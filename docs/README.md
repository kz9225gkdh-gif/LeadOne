# GitHub Pages 배포 메모

이 폴더는 `LeadOne`의 공개 지원 페이지와 개인정보 처리방침 페이지를 위한 GitHub Pages 소스입니다.

## 현재 페이지 경로

- 지원 페이지: `docs/leadone/support/index.html`
- 개인정보 처리방침 페이지: `docs/leadone/privacy/index.html`

## 예상 공개 URL 형식

GitHub Pages를 현재 저장소의 `main` 브랜치 `/docs` 폴더로 설정하면 아래처럼 접근할 수 있습니다.

- Support URL: `https://<github-username>.github.io/<repository-name>/leadone/support/`
- Privacy Policy URL: `https://<github-username>.github.io/<repository-name>/leadone/privacy/`

## 배포 방법

1. GitHub 저장소의 `Settings`로 이동합니다.
2. `Pages` 메뉴를 선택합니다.
3. Source를 `Deploy from a branch`로 설정합니다.
4. Branch는 `main`, folder는 `/docs`를 선택합니다.
5. 저장한 뒤 생성되는 공개 URL을 확인합니다.

## 현재 반영된 문의 이메일

- 지원 페이지 이메일: `wnsgur9359@naver.com`
- 개인정보 처리방침 페이지 이메일: `wnsgur9359@naver.com`
